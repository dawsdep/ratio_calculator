# find diretory install.sh is located at
# check that there is a release/binary/etc? located there too
# get the first argument the user passed in and set as install path
# if no passed path then prompt user
# check that directory exists at that path and if not create it
# cp the release to the provided directory path
# output success message?



# OLD
# pass in location to install entry executable or if not passed in prompt the user at the command line to enter a path
# get the directory the init.sh script is running from and located in
# use that to cp the ratio_calc.sh script into the path provided
#   check if path exists and create it if it doesnt (prompt to create?)